<x-layouts.app title="Staff & access">
    <div class="mb-7 flex flex-wrap items-center justify-between gap-4"><h1 class="text-3xl font-bold">Staff &amp; access</h1><div class="flex flex-wrap gap-3"><x-action-link :href="route('roles.index')" variant="secondary">Role permissions</x-action-link><x-action-link :href="route('users.create')">Add staff member</x-action-link></div></div>
    <p class="mb-5 text-sm text-text-secondary">Deactivate accounts instead of deleting them. Transaction history stays linked to the original staff member.</p>
    <form method="GET" class="mb-6 flex flex-wrap items-end gap-4"><x-input name="q" label="Name or email" :value="$filters['q'] ?? ''" maxlength="150" /><x-select name="status" label="Status"><option value="">All accounts</option><option value="active" @selected(($filters['status'] ?? '') === 'active')>Active</option><option value="inactive" @selected(($filters['status'] ?? '') === 'inactive')>Inactive</option></x-select><x-select name="role_id" label="Role"><option value="">All roles</option>@foreach($roles as $role)<option value="{{ $role->id }}" @selected(($filters['role_id'] ?? '') == $role->id)>{{ $role->name }}</option>@endforeach</x-select><x-button type="submit" variant="secondary">Filter</x-button></form>
    <div class="overflow-hidden rounded-xl border border-border bg-surface"><div class="relative overflow-x-auto"><table class="w-full min-w-[650px] text-left text-sm"><caption class="sr-only">Staff accounts</caption><thead><tr>@foreach(['Name', 'Email', 'Role', 'Status', 'Last sign-in'] as $label)<th scope="col" class="p-4">{{ $label }}</th>@endforeach</tr></thead><tbody class="divide-y divide-border">@forelse($users as $user)<tr><th scope="row" class="p-4 font-medium"><a class="underline" href="{{ route('users.edit', $user) }}">{{ $user->name }}</a></th><td class="break-all p-4">{{ $user->email }}</td><td class="p-4">{{ $user->role?->name ?? 'No role' }}</td><td class="p-4">{{ $user->is_active ? 'Active' : 'Inactive' }}</td><td class="p-4">{{ $user->last_login_at?->format('d M Y H:i') ?? 'Never' }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center">No staff accounts found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $users->links() }}</div></div>
</x-layouts.app>
