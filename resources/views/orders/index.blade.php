<x-layouts.app title="Orders">
    <div class="mb-7 flex flex-wrap items-center justify-between gap-4"><h1 class="text-3xl font-bold">Orders</h1><x-action-link :href="route('orders.create')">New order</x-action-link></div>
    <form method="GET" class="mb-6 grid gap-4 sm:grid-cols-3"><x-input name="q" label="Number or customer" :value="$filters['q'] ?? ''" maxlength="191" /><x-select name="status" label="Status"><option value="">All statuses</option>@foreach(\App\Enums\OrderStatus::cases() as $status)<option value="{{ $status->value }}" @selected(($filters['status'] ?? '') === $status->value)>{{ str_replace('_', ' ', $status->value) }}</option>@endforeach</x-select><div class="self-end"><x-button type="submit" variant="secondary">Filter</x-button></div></form>
    <div class="overflow-hidden rounded-xl border border-border bg-surface"><div class="relative overflow-x-auto"><table class="w-full min-w-[650px] text-left text-sm"><caption class="sr-only">Customer orders</caption><thead class="border-b border-border bg-background"><tr>@foreach(['Order', 'Customer', 'Status', 'Payment', 'Total (TZS)'] as $label)<th scope="col" class="p-5">{{ $label }}</th>@endforeach</tr></thead><tbody class="divide-y divide-border">@forelse($orders as $order)<tr><th scope="row" class="p-5 font-medium"><a href="{{ route('orders.show', $order) }}" class="underline">{{ $order->order_number }}</a></th><td class="p-5">{{ $order->customer->full_name }}</td><td class="p-5">{{ str_replace('_', ' ', $order->status->value) }}</td><td class="p-5">{{ $order->payment_status }}</td><td class="p-5">{{ \App\Support\Money::format($order->total_amount, false) }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-text-secondary">No orders found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-border p-5">{{ $orders->links() }}</div></div>
</x-layouts.app>
