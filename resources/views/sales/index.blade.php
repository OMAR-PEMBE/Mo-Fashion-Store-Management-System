<x-layouts.app title="Sales history">
    <div class="mb-7 flex flex-wrap items-center justify-between gap-4"><h1 class="text-3xl font-bold">Sales history</h1><x-action-link :href="route('sales.create')">New sale</x-action-link></div>
    <form method="GET" class="mb-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><x-input name="q" label="Number or customer" :value="$filters['q'] ?? ''" maxlength="191" /><x-select name="payment_method" label="Payment method"><option value="">All methods</option>@foreach(\App\Services\SaleService::PAYMENT_METHODS as $value => $label)<option value="{{ $value }}" @selected(($filters['payment_method'] ?? '') === $value)>{{ $label }}</option>@endforeach</x-select><x-input type="date" name="date_from" label="From date" :value="$filters['date_from'] ?? ''" /><x-input type="date" name="date_to" label="To date" :value="$filters['date_to'] ?? ''" /><x-button type="submit" variant="secondary">Filter</x-button></form>
    <div class="overflow-hidden rounded-xl border border-border bg-surface"><div class="relative overflow-x-auto"><table class="w-full min-w-[650px] text-left text-sm"><caption class="sr-only">Completed sales</caption><thead class="border-b border-border bg-background"><tr>@foreach(['Sale / Date', 'Customer', 'Staff', 'Payment', 'Total (TZS)'] as $label)<th scope="col" class="p-5">{{ $label }}</th>@endforeach</tr></thead><tbody class="divide-y divide-border">@forelse($sales as $sale)<tr><th scope="row" class="p-5 font-medium"><a href="{{ route('sales.show', $sale) }}" class="underline">{{ $sale->sale_number }}</a><span class="mt-1 block text-xs text-text-secondary">{{ $sale->sale_date->format('d M Y H:i') }}</span></th><td class="p-5">{{ $sale->customer?->full_name ?? 'Walk-in' }}</td><td class="p-5">{{ $sale->salesperson->name }}</td><td class="p-5">{{ \App\Services\SaleService::PAYMENT_METHODS[$sale->payment_method] }}</td><td class="p-5">{{ $sale->total_amount }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-text-secondary">No sales found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-border p-5">{{ $sales->links() }}</div></div>
</x-layouts.app>
