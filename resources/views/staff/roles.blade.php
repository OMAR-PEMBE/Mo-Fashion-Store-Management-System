<x-layouts.app title="Role permissions"><a href="{{ route('users.index') }}" class="text-sm underline">Staff &amp; access</a><h1 class="mb-6 mt-2 text-3xl font-bold">Role permissions</h1><p class="mb-5 text-sm text-text-secondary">Permissions apply to every member of a role. Changes take effect on subsequent requests.</p><div class="grid gap-5 sm:grid-cols-2">@foreach($roles as $role)<x-card :title="$role->name"><p class="mb-4 text-sm">{{ $role->users_count }} staff accounts</p><a class="text-sm underline" href="{{ route('roles.edit', $role) }}">Review permissions</a></x-card>@endforeach</div></x-layouts.app>
