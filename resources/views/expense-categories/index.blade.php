<x-layouts.app title="Expense categories">
    
    <a href="{{ route('expenses.index') }}" class="text-sm underline">Expenses</a><div class="mb-7 mt-2 flex flex-wrap items-center justify-between gap-4"><h1 class="text-3xl font-bold">Expense categories</h1><x-action-link :href="route('expense-categories.create')">Add category</x-action-link></div>
    <form method="GET" class="mb-6 flex flex-wrap items-end gap-4"><x-input name="q" label="Search categories" :value="$filters['q'] ?? ''" maxlength="150" /><x-select name="status" label="Status"><option value="">All categories</option><option value="active" @selected(($filters['status'] ?? '') === 'active')>Active</option><option value="inactive" @selected(($filters['status'] ?? '') === 'inactive')>Inactive</option></x-select><x-button type="submit" variant="secondary">Filter</x-button></form>
    <div class="space-y-4">@forelse($categories as $category)<x-card><div class="flex flex-wrap items-center justify-between gap-3"><a href="{{ route('expense-categories.edit', $category) }}" class="break-words font-semibold underline">{{ $category->name }}</a><x-badge :tone="$category->is_active ? 'success' : 'warning'">{{ $category->is_active ? 'Active' : 'Inactive' }}</x-badge></div><p class="mt-3 break-words text-sm text-text-secondary">{{ $category->description }}</p></x-card>@empty<x-card><p class="text-sm">No expense categories found.</p></x-card>@endforelse</div><div class="mt-5">{{ $categories->links() }}</div>
</x-layouts.app>
