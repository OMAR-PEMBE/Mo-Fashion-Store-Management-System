<x-layouts.app title="Refunds">
    <div class="mb-7 flex flex-wrap items-center justify-between gap-4"><h1 class="text-3xl font-bold">Refunds</h1><x-action-link :href="route('refunds.create')">Request refund</x-action-link></div>
    <form method="GET" class="mb-6 grid gap-4 sm:grid-cols-3"><x-input name="q" label="Refund or sale number" :value="$filters['q'] ?? ''" maxlength="191" /><x-select name="status" label="Status"><option value="">All statuses</option>@foreach(\App\Enums\RefundStatus::cases() as $status)<option value="{{ $status->value }}" @selected(($filters['status'] ?? '') === $status->value)>{{ $status->value }}</option>@endforeach</x-select><div class="self-end"><x-button type="submit" variant="secondary">Filter</x-button></div></form>
    <div class="relative overflow-x-auto rounded-xl border border-border bg-surface"><table class="w-full min-w-[550px] text-left text-sm"><caption class="sr-only">Money refund history</caption><thead><tr>@foreach(['Refund', 'Original sale', 'Status', 'Amount (TZS)', 'Date'] as $label)<th scope="col" class="p-5">{{ $label }}</th>@endforeach</tr></thead><tbody>@forelse($refunds as $refund)<tr class="border-t border-border"><th scope="row" class="p-5 font-medium"><a href="{{ route('refunds.show', $refund) }}" class="underline">{{ $refund->refund_number }}</a></th><td class="p-5">{{ $refund->sale->sale_number }}</td><td class="p-5">{{ $refund->status->value }}</td><td class="p-5">{{ \App\Support\Money::format($refund->amount, false) }}</td><td class="p-5">{{ $refund->created_at->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-text-secondary">No refunds found.</td></tr>@endforelse</tbody></table><div class="border-t border-border p-5">{{ $refunds->links() }}</div></div>
</x-layouts.app>
